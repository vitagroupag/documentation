apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hip-cdr-docs.fullname" . }}-test-connection"
  labels:
    {{- include "hip-cdr-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hip-cdr-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
