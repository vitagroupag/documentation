apiVersion: v1
kind: Service
metadata:
  name: {{ include "hip-cdr-docs.fullname" . }}
  labels:
    {{- include "hip-cdr-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hip-cdr-docs.selectorLabels" . | nindent 4 }}
